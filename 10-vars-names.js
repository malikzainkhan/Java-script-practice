// List of variable names 

// Valid variable names
var myVar;       // Valid (Starts with a letter, followed by letters)
var _temp;       // Valid (Starts with an underscore)
var $price;      // Valid (Starts with a dollar sign)
var userName;    // Valid (Camel case, starts with a letter)
var user123;     // Valid (Starts with a letter, followed by numbers)
var user_name;   // Valid (Starts with a letter, contains an underscore)

 //Invalid variable names
//  var 2ndVar;    // Invalid (Cannot start with a number)
//  var user-name; // Invalid (Hyphens are not allowed in variable names)
// var user name;  // Invalid (Spaces are not allowed in variable names)
//  var user@name; // Invalid (Special characters like @ are not allowed)//